# useFetch Hook

Ejemplo de uso:
```
    const url = 'endpoint de una API'
    const { data, loading, error } = useFetch( 10 );
```

Objeto por defecto:
```
    data: null
    loading: true,
    error: null
```
