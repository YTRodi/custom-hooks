# useFetch Hook

Ejemplo de uso:
```
    const url = 'endpoint de una API'
    const { data, loading, error } = useCounter( 10 );
```

Objeto por defecto:
```
    data: null
    loading: true,
    error: null
```